import React from 'react';

export default function Landing() {
  return (
    <div style={{ textAlign: 'center', padding: 40 }}>
      <h1>Welcome to qgraph-migrate</h1>
      <p>This page was server-rendered by React + Express + Vite.</p>
      <a href="/app">Go into the SPA →</a>
    </div>
  );
}
